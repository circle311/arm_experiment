# 智能联网时钟系统完整实施方案

## 一、项目核心要求

这个项目不是单纯做一个电子钟，而是要做一个 **S800 板本地可独立运行 + PC 上位机双向镜像控制** 的智能联网时钟系统。

项目由三大部分组成：

### 1. S800 板端基础功能，必做，占 52 分

- 开机动画：数码管和 LED 全亮/全灭、显示学号后 8 位、姓名拼音、版本号。
- 时钟与日期：`HH.MM.SS`、`YY.MM.DD`、`YYYY.MMDD` 切换显示。
- 流水显示：超过 8 位内容自动滚动，支持 `LEFT/RIGHT` 方向和 2 档速度。
- 闹钟：时分秒匹配触发，蜂鸣器节奏响铃，`FUNC` 可立即停止。
- 按键编辑状态机：设置日期、时间、闹钟，支持字段切换、加一、保存、5 秒无操作退出。
- LED 状态指示：至少包含心跳、闹钟、编辑模式、串口收发活动。

### 2. 串口协议，必做，占 10 分

- 波特率 `115200 8N1`，ASCII 编码。
- 支持 `*RST`、`*SET`、`*GET`、`*PING`。
- 支持大小写不敏感、空格容错、缩写命令。
- S800 主动上报 `*EVT:DISP`、`*EVT:LED`、`*EVT:KEY`、`*EVT:MODE` 等事件。
- `*EVT:DISP` 和 `*EVT:LED` 每秒主动上报一次，作为 PC 镜像同步心跳。

### 3. PC 上位机，必做，占 14 分

- 串口扫描、连接、断开、后台接收。
- 可视化控制面板，能发送所有协议命令。
- 数字孪生镜像面板：8 位数码管、8 位 LED、8 个按键、USER1、USER2。
- 板端变化同步到 PC，PC 点击虚拟按键也能控制板端。
- 收发日志、错误处理、状态栏显示。

### 高分要求

如果想冲 A/A+，还必须完成：

- 至少 2 个扩展功能，建议选 **E1 网络对时 + E2 天气获取**。
- 至少 1 个自主增加功能。
- 报名并通过答辩。

## 二、推荐总体架构

建议把系统分成 MCU 工程和 PC 上位机工程：

```text
大作业<学号>-<姓名>/
├── mcu/
│   ├── Inc/
│   ├── Driverlib/
│   ├── src/main.c
│   ├── src/...
│   └── obj/xxx.axf
├── pc_host/
│   ├── main.py
│   ├── ui_main.py 或 main.ui
│   ├── serial_worker.py
│   ├── protocol.py
│   ├── widgets.py
│   └── requirements.txt
├── docs/
│   ├── 大作业<学号>-<姓名>.pdf
│   └── 演示视频.mp4
└── README.md
```

文档要求“所有自编代码集中放 `main.c`”，所以 MCU 侧如果老师严格检查，最好把核心业务逻辑都放进 `src/main.c`，其他文件只保留课程给的驱动库或必要头文件。

## 三、S800 板端实施步骤

板端是最高分主体，建议优先做稳。

### 第 1 步：建立统一状态结构

```c
typedef struct {
    int year, month, day;
    int hour, min, sec;
    int alarm_hour, alarm_min, alarm_sec;
    int alarm_enabled;
    int alarm_ringing;

    int display_mode;     // TIME / DATE / YEAR
    int edit_mode;        // NONE / DATE / TIME / ALARM
    int edit_field;
    int format_right;     // LEFT=0, RIGHT=1
    int scroll_speed;     // 0/1
    int display_on;
    int mode_night;       // DAY/NIGHT

    uint8_t led_state;
    char msg[33];
} ClockState;
```

### 第 2 步：实现 1 ms 系统节拍

需要完成：

- 1 ms tick 计数。
- 每 1000 ms 秒加一。
- 日期进位、闰年判断。
- 按键扫描与消抖。
- 5 秒编辑超时。
- 闹钟响铃计时。
- 每秒发送一次 `*EVT:DISP` 和 `*EVT:LED`。

### 第 3 步：实现显示层

显示层建议做成统一函数：

```c
void display_render(void);
void display_set_chars(char chars[8], uint8_t dp_mask);
void display_apply_format(char *in, uint8_t in_dp, char *out, uint8_t *out_dp);
```

重点是 `FORMAT RIGHT`：

- 时间 `12.30.45`
- `LEFT` 显示/应答：`12.30.45`
- `RIGHT` 显示/应答：`54.03.21`
- 小数点按“下一位规则”跟随。

这是硬指标，必须单独测试。

### 第 4 步：实现按键状态机

| 按键 | 功能 |
|---|---|
| `FUNC` | 进入/切换编辑模式；响铃时关闭闹钟 |
| `SHIFT` | 切换编辑字段 |
| `ADD` | 当前字段 +1 |
| `SAVE` | 保存退出 |
| `DISP` | 时间/日期/年份切换 |
| `SPEED` | 流水速度切换 |
| `FORMAT` | 左/右流水切换 |
| `EXT` | 上报 `*EVT:KEY EXT` |
| `USER1` | 上报 `*EVT:KEY USER1`，触发 PC 对时 |
| `USER2` | 显示天气 5 秒 |

### 第 5 步：实现串口协议解析

建议流程：

```text
UART 接收字符
→ 收到 \r 或 \n 组成一行
→ 去掉首尾空白
→ 按空格/Tab 分词
→ 命令统一转大写用于比较
→ 根据 *RST / *SET / *GET / *PING 分派
→ 成功返回 OK
→ 失败返回 ERROR <reason>
```

必须支持：

- 大小写不敏感。
- 多空格/Tab。
- 缩写：如 `MIN`、`MINU`、`MINUT`、`MINUTE` 都合法。
- 错误必须返回 `ERROR SYNTAX/PARAM/RANGE/LEN/BUSY` 之一。

## 四、PC 上位机实施步骤

推荐技术栈：`Python + PyQt5 + pyserial`。

### 第 1 步：搭建 UI

主界面建议分 4 块：

```text
顶部：串口选择、刷新、连接、断开、状态栏
左侧：控制面板，发送 *SET / *GET / *RST / *PING
中间：数字孪生镜像面板
底部：收发日志
右侧或标签页：扩展功能与自主功能
```

### 第 2 步：串口线程

用独立线程读串口，避免 GUI 卡死：

```text
SerialWorker
├── scan_ports()
├── open(port, baud=115200)
├── close()
├── send_line(text)
└── read_loop()
```

接收到一行后发信号给主线程：

```python
line_received.emit(line)
```

### 第 3 步：协议解析

PC 侧写一个 `protocol.py`：

```python
def parse_line(line):
    if line.startswith("*EVT:DISP"):
        return DispEvent(chars, dp_hex)
    if line.startswith("*EVT:LED"):
        return LedEvent(hex2)
    if line.startswith("*EVT:KEY"):
        return KeyEvent(name)
    if line.startswith("*PONG"):
        return PongEvent(uptime)
    if line.startswith("OK"):
        return OkResponse(data)
    if line.startswith("ERROR"):
        return ErrorResponse(reason)
```

### 第 4 步：数字孪生面板

需要完整展示：

- 8 位 7SEG，可用自绘 QWidget，也可以用 QLabel 模拟。
- 8 位 LED，圆点或小灯。
- 8 个普通按键：`FUNC` 到 `EXT`。
- 2 个独立按键：`USER1`、`USER2`。

点击虚拟按键时发送：

```text
*SET:KEY FUNC
*SET:KEY SHIFT
*SET:KEY ADD
...
*SET:KEY USER1
*SET:KEY USER2
```

注意：PC 虚拟按键触发后，S800 不应再回传 `*EVT:KEY`，避免环回。

### 第 5 步：日志与异常处理

日志每行建议包含：

```text
[12:30:45.123] TX  *SET:TIME HOUR MIN SEC 12 30 45
[12:30:45.140] RX  OK
[12:30:46.000] EVT *EVT:DISP 123045__ 14
[12:30:46.003] ERR ERROR PARAM
```

颜色建议：

- TX：蓝色
- RX OK：绿色
- EVT：紫色或灰色
- ERROR：红色

## 五、扩展功能建议

为了高分，建议做这两个：

### 1. E1 网络对时，2 分

- PC 点击“NTP 对时”按钮。
- 获取当前网络/系统时间。
- 下发：

```text
*SET:DATE YEAR MONTH DATE 2026 06 07
*SET:TIME HOUR MIN SEC 14 30 00
```

- 当 S800 上按 `USER1` 时，上报：

```text
*EVT:KEY USER1
```

- PC 自动执行同样对时流程。

### 2. E2 天气获取，3 分

- PC 获取天气，或者没有网络时允许手动输入天气。
- 下发滚动消息：

```text
*SET:MSG 28C SUNNY
```

- S800 按 `USER2` 后短显天气 5 秒。
- PC 镜像跟随 `*EVT:DISP` 自动更新。

这样扩展功能能拿到 5 分，满足 A/A+ 高分门槛里的“至少完成 2 项扩展，得分 ≥ 4”。

## 六、自主增加功能建议

推荐做一个容易实现、容易演示、又有工程价值的功能：

**自主功能：一键自动化验收脚本 / 回归测试面板**

PC 端增加“自动验收”按钮，自动发送一组测试命令：

```text
*PING
*SET:DATE YEAR MONTH DATE 2026 06 01
*SET:TIME HOUR MIN SEC 12 30 45
*GET:TIME
*SET:FORMAT RIGHT
*GET:TIME
*SET:DISP OFF
*SET:DISP ON
*SET:LED FF
*SET:BEEP 500
*SET:KEY DISP
```

然后自动判断：

- 是否收到 `OK`
- `RIGHT` 模式是否返回逆序时间
- 是否收到 `*EVT:DISP`
- 是否收到 `*EVT:LED`
- 错误命令是否返回 `ERROR`

最后生成测试报告：

```text
通过 12/13
失败项：FORMAT RIGHT 小数点跟随
测试时间：2026-06-07 14:30:00
```

这个功能的优点是：

- 不和原功能重复。
- 很容易在视频中演示。
- 对答辩很有帮助。
- 可以写进 PDF 的“动机—设计—实现—演示”。

## 七、推荐开发流程

按 3 周推进最稳。

### 第 1 周：板端基础跑起来

目标：

- 工程能编译、烧写。
- 数码管能稳定显示。
- 1 ms tick 正常。
- 时间能走。
- 日期进位正确。
- 开机动画完成。
- UART 能接收一行并回显。

验收：

```text
上电显示学号/姓名/版本号
进入 HH.MM.SS 正常走时
串口输入 *PING 返回 *PONG
```

### 第 2 周：板端业务 + 协议完整

目标：

- 闹钟功能。
- 编辑状态机。
- 按键完整映射。
- 流水显示。
- `FORMAT LEFT/RIGHT`。
- `*RST`、`*SET`、`*GET`、`*PING` 全部完成。
- 每秒上报 `*EVT:DISP` 和 `*EVT:LED`。
- 启动自主功能原型。

验收：

```text
不接 PC 时，S800 可完成 §3 全部功能
串口助手能测试 §5.1-§5.2 全部命令
FORMAT RIGHT 显示和应答都正确
```

### 第 3 周：PC 联调 + 扩展 + 文档视频

目标：

- PC 串口连接。
- 控制面板。
- 数字孪生镜像。
- 日志。
- 异常处理。
- E1 网络对时。
- E2 天气获取。
- 自主功能自动化验收。
- 写 README、简介 PDF，录视频。

验收：

```text
PC 镜像面板与 S800 实物同步
PC 点击虚拟按键，S800 有等效反应
S800 物理按键变化，PC 能收到事件并更新
演示视频覆盖 §3、§4.2、§4.3
```

## 八、最小可交付版本

如果时间紧，优先级如下：

1. S800 本地时钟、日期、开机画面。
2. 按键设置和闹钟。
3. 串口协议 `*SET` / `*GET` / `*PING`。
4. `*EVT:DISP` / `*EVT:LED` 每秒上报。
5. PC 串口连接和日志。
6. PC 数字孪生镜像。
7. E1 网络对时。
8. E2 天气获取。
9. 自主功能自动化验收。

不要先做花哨 UI。这个项目评分核心是：**板端稳定、协议完整、PC 镜像同步、材料齐全**。UI 可以朴素，但功能链路必须通。

## 九、固定工作约定

后续每次开始实现新功能或修改代码前，先回顾：

1. `完整实施方案.md`
2. `工作日志.md`
3. `大作业题目-学生版_V1.2.md`

然后再根据当前阶段目标动代码。每完成一个大步骤后，继续维护 `工作日志.md`。

### PC 端开发环境确认

后续每次执行 PC 端开发、运行、测试、依赖安装、UI 编译或串口调试前，必须先确认当前 PowerShell 是否处于项目虚拟环境：

```powershell
$env:VIRTUAL_ENV
python --version
pip --version
```

成功状态应满足：

- `$env:VIRTUAL_ENV` 指向 `F:\program\ARM\Task\pc_host\.venv`
- `python --version` 输出 `Python 3.11.9`
- 架构检查为 `64bit / 64`

推荐直接运行统一验证脚本：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_host\verify_env.ps1
```

如果当前不处于虚拟环境，先进入虚拟环境再继续：

```powershell
.\pc_host\.venv\Scripts\Activate.ps1
```

不确认虚拟环境、不激活虚拟环境时，不继续执行 PC 端 Python 命令。
