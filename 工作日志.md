# 工作日志

## 固定工作约定

后续每次开始实现新功能或修改代码前，先回顾：

1. `完整实施方案.md`
2. `工作日志.md`
3. `大作业题目-学生版_V1.2.md`

然后再根据当前阶段目标动代码。每完成一个大步骤后，继续维护 `工作日志.md`。

PC 端开发、运行、测试、依赖安装、UI 编译或串口调试前，必须先确认是否处于 `pc_host/.venv` 虚拟环境；若未处于虚拟环境，则先执行：

```powershell
.\pc_host\.venv\Scripts\Activate.ps1
```

推荐使用统一验证脚本：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_host\verify_env.ps1
```

确认 Python 为 3.11.9，架构为 64bit / 64 后，再继续执行 PC 端相关命令。

## 2026-06-09 补充虚拟环境执行前确认规则

### 本次目标

将“每次 PC 端开发前确认虚拟环境，若未进入则先进入虚拟环境”的规则写入实施方案，并同步维护工作日志。

### 已完成文件

- `完整实施方案.md`
- `工作日志.md`

### 已完成内容

1. 在 `完整实施方案.md` 的固定工作约定中补充 PC 端开发环境确认规则。
2. 在 `工作日志.md` 的固定工作约定中补充相同规则。
3. 本次执行前已运行 `pc_host/verify_env.ps1`，确认当前虚拟环境正常：

```text
VIRTUAL_ENV = F:\program\ARM\Task\pc_host\.venv
Python 3.11.9
pip 24.0
arch = 64bit | bits = 64
PyQt5 5.15.2 | pyserial 3.5 | OK
```

## 2026-06-09 GitHub 远程仓库配置与推送

### 本次目标

将本地 git 仓库关联到用户提供的 GitHub 仓库，并完成首次推送。

### 已完成内容

1. 配置远程仓库：

```text
origin https://github.com/circle311/arm_experiment.git
```

2. 本地提交记录：

```text
5527fa8 Document virtual environment workflow
3cf6e47 Add PC environment verification
ba26ce1 Initialize project environment
```

3. 已成功推送：

```text
master -> origin/master
```

### 当前状态

本地 `master` 分支已跟踪 `origin/master`。后续完成大步骤后，继续提交并推送即可。

## 2026-06-09 PC 端 Python 环境初始化

### 本次目标

在 `pc_host` 中建立 PC 上位机开发环境：创建虚拟环境、安装 PyQt5/串口/网络/绘图库依赖、生成依赖清单、加入 `.gitignore`，并初始化本地 git 仓库。

### 已完成文件

- `.gitignore`
- `pc_host/requirements.txt`
- `pc_host/verify_env.ps1`
- `pc_host/.venv/`（已被 `.gitignore` 排除，不提交）
- `.git/`（本地 git 仓库）

### 已完成内容

1. 安装 Python 解释器：
   - 安装位置：`F:\Python3119`
   - 版本：Python 3.11.9
   - 架构：64bit / 64
   - pip：24.0

2. 创建虚拟环境：
   - 位置：`pc_host/.venv`
   - 后续开发前必须确认使用该虚拟环境。
   - 推荐激活命令：

```powershell
.\pc_host\.venv\Scripts\Activate.ps1
```

3. 安装 PC 端必装包：
   - `pyqt5`
   - `pyqt5-tools`
   - `pyserial`
   - `ntplib`
   - `requests`
   - `astral`
   - `matplotlib`

4. 验证结果：

```text
Python 3.11.9
pip 24.0
arch = 64bit | bits = 64
PyQt5 5.15.2 | pyserial 3.5 | OK
Python User Interface Compiler 5.15.9 for Qt version 5.15.2
```

5. Qt Designer 路径：

```text
F:\program\ARM\Task\pc_host\.venv\Lib\site-packages\pyqt5_tools\Qt\bin\designer.exe
```

6. 新增开发前环境验证脚本：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_host\verify_env.ps1
```

该脚本会自动激活 `pc_host/.venv`，并验证 Python、pip、64 位架构、PyQt5、pyserial、Designer 路径和 `pyuic5`。

7. 生成冻结版依赖清单：

```powershell
.\pc_host\.venv\Scripts\python.exe -m pip freeze > .\pc_host\requirements.txt
```

8. 初始化本地 git：
   - 已执行 `git init`
   - 已创建提交：`ba26ce1 Initialize project environment`

### 当前限制

远程仓库尚未配置，因此目前只能完成本地 git 初始化与提交，暂时无法执行上传。需要用户提供 GitHub/Gitee 等远程仓库地址后，才能继续执行：

```powershell
git remote add origin <远程仓库地址>
git push -u origin master
```

### 下一步计划

拿到远程仓库地址后，继续配置 `origin` 并推送。之后进入 PC 上位机代码开发前，先激活并确认虚拟环境：

```powershell
.\pc_host\.venv\Scripts\Activate.ps1
python --version
```

## 2026-06-09 完整实施方案落盘

### 本次目标

将此前整理的完整实施方案保存为独立 Markdown 文档，并建立后续工作前的固定回顾约定。

### 已完成文件

- `完整实施方案.md`
- `工作日志.md`

### 已完成内容

1. 新增 `完整实施方案.md`，内容包括：
   - 项目核心要求。
   - 推荐工程架构。
   - S800 板端实施步骤。
   - PC 上位机实施步骤。
   - 扩展功能建议。
   - 自主增加功能建议。
   - 三周开发流程。
   - 最小可交付版本优先级。
   - 固定工作约定。

2. 更新 `工作日志.md`：
   - 增加“固定工作约定”。
   - 明确后续每次工作前先回顾 `完整实施方案.md`、`工作日志.md`、`大作业题目-学生版_V1.2.md`。

### 下一步计划

下次进入代码实现前，先按固定工作约定回顾三个文档，再继续推进第二周目标。

## 2026-06-09 第一周目标代码基线

### 本次目标

完成大作业第一周阶段目标所需的 MCU 侧基础代码，为后续接入真实 S800/Keil 底层驱动做准备。

第一周目标包括：

- 搭建主工程基础目录。
- 完成开机画面。
- 完成基础时钟走时。
- 完成日期/时间显示。
- 完成 UART 按行接收与回显。
- 给后续真实硬件驱动接入预留清晰接口。

### 已完成文件

- `mcu/src/main.c`
- `README.md`

### 已完成内容

1. 创建工程目录结构：
   - `mcu/src/`
   - `mcu/Inc/`
   - `mcu/obj/`
   - `pc_host/`
   - `docs/`

2. 在 `mcu/src/main.c` 中实现第一周 MCU 基线逻辑：
   - 上电开机动画。
   - 8 位数码管与 8 位 LED 全亮/全灭。
   - 显示学号后 8 位。
   - 显示姓名拼音。
   - 显示版本号。
   - 进入正常时钟显示。
   - 基于 1 ms 系统时基累计秒。
   - 支持年月日进位。
   - 支持闰年判断。
   - 支持 `HH.MM.SS`、短日期、长日期显示模式。
   - 支持 UART 行接收、回显和简单调试命令。

3. 当前 UART 调试命令：
   - `*PING`
   - `*GET:TIME`
   - `*GET:DATE`
   - `DISP`

4. 预留硬件适配层：
   - `Board_Init`
   - `Board_Millis`
   - `Board_DelayMs`
   - `Board_Seg7Show`
   - `Board_LedWrite`
   - `Board_UartReadByte`
   - `Board_UartWriteByte`
   - `Board_UartWriteString`

5. 在 `README.md` 中补充：
   - 第一周已覆盖目标。
   - 需要接入的底层函数。
   - 与 S800 课程实验驱动的建议映射方式。
   - 编译、烧写与验收建议。
   - 第二周继续开发方向。

### 验证结果

已使用本机 GCC 做 C 语法检查：

```bash
gcc -std=c99 -Wall -Wextra -fsyntax-only .\mcu\src\main.c
```

检查结果：通过，无语法错误。

### 当前限制

当前工作区没有课程提供的 S800/Keil 底层驱动工程，因此 `main.c` 中的 `Board_*` 函数目前是弱定义占位实现。真实上板前需要将这些函数替换为课程实验阶段已有的数码管、LED、UART、SysTick 等驱动调用。

### 下一步计划

第二周建议继续完成：

- 真实按键扫描与消抖。
- 闹钟功能。
- 编辑状态机。
- 流水显示。
- `FORMAT LEFT/RIGHT` 及小数点跟随。
- 完整 `*SET` / `*GET` 协议解析。
- 串口容错规则：大小写不敏感、空格容错、缩写合法。
- `*EVT:DISP` / `*EVT:LED` 每秒心跳上报。

## 2026-06-09 S800/Keil 工程接入

### 本次目标

检查用户复制到 `ARM/` 目录的 Keil 工程，明确是否需要新建工程，以及第一周代码中的 `Board_*` 如何接入真实 S800 硬件驱动。

### 已完成文件

- `ARM/exp3-1.c`
- `ARM/Board接入说明.md`
- `.gitignore`
- `工作日志.md`

### 已完成内容

1. 检查 `ARM/exp3.uvprojx`，确认工程已经包含：
   - `PART_TM4C1294NCPDT` 编译宏。
   - `.\inc;.\driverlib` 头文件路径。
   - `driverlib\rvmdk\driverlib.lib`。
   - `RTE\Device\TM4C1294NCPDT` 启动和系统文件。
   - 用户源文件 `.\exp3-1.c`。
2. 将第一周 `mcu/src/main.c` 的通用业务逻辑移植到 `ARM/exp3-1.c`：
   - 开机动画。
   - `HH.MM.SS` 走时。
   - 短日期/长日期切换。
   - UART 行接收与调试命令。
   - `*PING`、`*GET:TIME`、`*GET:DATE`、`DISP`。
   - 兼容原例程 `AT+CLASS`、`AT+STUDENTCODE`。
3. 将 `Board_*` 接入到真实 S800/TM4C1294 驱动：
   - `Board_Init` -> 系统时钟、GPIO、I2C0、UART0、SysTick 初始化。
   - `Board_Millis` -> 1 ms SysTick 计数。
   - `Board_Seg7Show` -> 七段码缓存。
   - `SysTick_Handler` -> 1 ms 数码管动态扫描。
   - `Board_LedWrite` -> PCA9557 输出，低有效。
   - `Board_UartReadByte` / `Board_UartWriteByte` -> UART0。
4. 新增 `ARM/Board接入说明.md`，记录 Keil 替换方式、`Board_*` 映射关系和 SSCOM 验证步骤。
5. 更新 `.gitignore`，忽略 Keil 生成目录 `ARM/Objects/`、`ARM/Listings/` 和本机界面状态文件 `ARM/*.uvguix.*`。

### 验证结果

已执行本机 C 语法检查：

```powershell
gcc -std=c99 -Wall -Wextra -fsyntax-only -DPART_TM4C1294NCPDT -I.\ARM\inc -I.\ARM\driverlib .\ARM\exp3-1.c
```

结果：通过。

### 当前限制

本机没有实际 S800 板卡和 Keil 编译/烧录环境，因此还需要在 Keil 中 Build、Download，并用 SSCOM 做硬件验证。

## 2026-06-09 板端串口校时命令

### 本次目标

解释上板后 `*PING`、`*GET:DATE`、`*GET:TIME` 的现象，并补齐第一周代码中最需要的手动校时能力。

### 现象判断

- `*PING` 返回 `*PONG 0` 属于正常通信。
- 日期显示 `2026-06-01`、时间从 `12:00:00` 附近开始走，属于正常现象。
- 原因是当前阶段还没有 RTC、PC 自动同步或网络对时，板端只能从程序里的初始时间开始计时。

### 已完成文件

- `ARM/exp3-1.c`
- `ARM/Board接入说明.md`
- `工作日志.md`

### 已完成内容

1. 在 `ARM/exp3-1.c` 中新增串口手动校时命令：
   - `*SET:TIME=HH:MM:SS`
   - `*SET:DATE=YYYY-MM-DD`
   - 简写 `TIME=HH:MM:SS`
   - 简写 `DATE=YYYY-MM-DD`
2. 设置成功后立即更新板端状态并刷新数码管显示：
   - 时间设置成功返回 `OK TIME`。
   - 日期设置成功返回 `OK DATE`。
3. 增加基础格式校验：
   - 小时范围 `0..23`。
   - 分秒范围 `0..59`。
   - 日期范围包含闰年和每月天数判断。
4. 更新 `ARM/Board接入说明.md`，补充 SSCOM 校时命令和错误返回说明。

### 验证结果

已执行本机 C 语法检查：

```powershell
gcc -std=c99 -Wall -Wextra -fsyntax-only -DPART_TM4C1294NCPDT -I.\ARM\inc -I.\ARM\driverlib .\ARM\exp3-1.c
```

结果：通过。

### 下一步计划

上板重新 Build/Download 后，用 SSCOM 发送：

```text
*SET:DATE=2026-06-09
*SET:TIME=19:56:00
*GET:DATE
*GET:TIME
```

确认返回值与数码管显示一致。后续第二周再做 PC 自动对时或网络对时。

## 2026-06-09 第二周板端功能第一版

### 本次目标

在第一周可上板运行的基础上，推进第二周板端核心能力：真实按键扫描、编辑状态机、闹钟、流水显示、格式切换、事件上报和更完整的串口协议。

### 已完成文件

- `ARM/exp3-1.c`
- `ARM/Board接入说明.md`
- `.github/workflows/arm-syntax-check.yml`
- `工作日志.md`

### 已完成内容

1. 新增 GitHub Actions workflow：
   - 文件：`.github/workflows/arm-syntax-check.yml`
   - 功能：push / PR 涉及 `ARM/**` 时自动执行 C 语法检查。
2. 重构 `ARM/exp3-1.c` 为第二周状态机结构：
   - 增加统一 `ClockState`。
   - 增加时间、日期、闹钟、显示模式、编辑模式、流水方向、流水速度、右向格式等状态。
3. 接入按键扫描与消抖：
   - TCA6424 Port0 8 位按键映射为 `FUNC/SHIFT/ADD/SAVE/DISP/SPEED/FORMAT/EXT`。
   - GPIO `USER1/USER2` 映射为独立按键。
   - 每 10 ms 扫描，连续稳定后生成按下/松开事件。
4. 增加按键功能：
   - `FUNC`：进入/切换编辑模式；闹钟响铃时停止闹钟。
   - `SHIFT`：切换编辑字段。
   - `ADD`：当前字段加一。
   - `SAVE`：保存退出。
   - `DISP`：切换时间/短日期/长日期/消息显示。
   - `SPEED`：切换流水速度。
   - `FORMAT`：切换 LEFT/RIGHT 显示格式。
   - `EXT/USER1/USER2`：输出事件，其中 USER2 显示天气占位消息。
5. 增加闹钟：
   - 支持 `*SET ALARM=HH:MM:SS`。
   - 支持 `*SET ALARM=ON/OFF`。
   - 到点后进入响铃状态，`FUNC` 可停止。
   - 当前蜂鸣器输出暂用 `GPIOF PIN0` 作为板端蜂鸣/提示输出占位。
6. 增加流水显示与格式：
   - `*SET MSG=...`
   - `*SET DISP=MSG`
   - `*SET SCROLL=LEFT/RIGHT`
   - `*SET SPEED=SLOW/FAST`
   - `*SET FORMAT=LEFT/RIGHT`
7. 扩展串口协议：
   - `*PING`
   - `*RST`
   - `*GET TIME/DATE/ALARM/LED/DISP/MSG`
   - `*SET TIME/DATE/ALARM/DISP/FORMAT/SPEED/SCROLL/MSG/KEY`
   - 保留 `TIME=...`、`DATE=...`、`DISP`、`AT+CLASS`、`AT+STUDENTCODE` 兼容命令。
8. 增加主动事件：
   - 每秒 `*EVT:DISP ...`
   - 每秒 `*EVT:LED 0x..`
   - 按键 `*EVT:KEY <name> DOWN/UP`
   - 模式变化 `*EVT:MODE ...`

### 验证结果

已执行本机 C 语法检查：

```powershell
gcc -std=c99 -Wall -Wextra -fsyntax-only -DPART_TM4C1294NCPDT -I.\ARM\inc -I.\ARM\driverlib .\ARM\exp3-1.c
```

结果：通过。

### 当前限制

1. 蜂鸣器真实引脚没有在当前实验例程中明确给出，目前以 `GPIOF PIN0` 做提示输出占位。上板后如果蜂鸣器不响，需要根据课程原理图把 `Board_BuzzerWrite()` 改到真实蜂鸣器管脚。
2. TCA6424 8 位按键默认按 Port0 bit0..bit7 映射；如果实际键位顺序不同，上板后只需调整 `KeyId` 映射或 `Board_KeyRawMask()`。
3. `FORMAT RIGHT` 已实现整屏反向和小数点反向跟随，仍建议在实物上重点检查显示方向是否符合老师定义。

## 2026-06-10 第二周上板反馈修复

### 本次目标

处理上板后的三个问题：

- 板子一直输出 `*EVT:DISP` / `*EVT:LED`，影响 SSCOM 手动调试。
- 输入 `*GET TIME` 返回 `ERROR SYNTAX`。
- 闹钟到点后蜂鸣器没有响。

### 原因分析

1. `*EVT:DISP` / `*EVT:LED` 连续输出是第二周事件上报功能在工作，符合题目“每秒主动上报”的要求；但手动调试时需要可关闭。
2. `*GET TIME` 解析失败是 `consume_set_separator()` 的 bug：函数先跳过空格，导致空格分隔形式无法被识别。
3. `S800板介绍.pdf` 没有明确给出蜂鸣器专用管脚，只确认 `PWM0-3` 对应 `PF0-PF3`，且 PF0 同时对应 LED。因此此前用 PF0 作为蜂鸣器占位输出，不能保证真实蜂鸣器发声。

### 已完成文件

- `ARM/exp3-1.c`
- `ARM/Board接入说明.md`
- `工作日志.md`

### 已完成内容

1. 修复串口命令分隔符解析：
   - 支持 `*GET TIME`。
   - 支持 `*GET:TIME`。
   - 支持 `*SET TIME=19:56:00`。
   - 支持 `*SET:TIME=19:56:00`。
2. 新增事件上报开关：
   - `*SET EVT=ON`
   - `*SET EVT=OFF`
   - 默认关闭主动事件，避免 SSCOM 刷屏。
3. 修正 `*GET DISP`：
   - 事件关闭时也会返回当前显示状态，例如 `DISP TIME LEFT`。
4. 修正闹钟自动停止时间：
   - 从 60 秒改为 10 秒，满足题目“持续 ≤ 10 秒自动停止”要求。
5. 新增蜂鸣占位测试命令：
   - `*SET BEEP=ON`
   - `*SET BEEP=OFF`
   - 当前仍输出到 PF0，占位用于确认电平/LED变化；真实蜂鸣器管脚待课程例程或原理图确认。
6. 更新 `ARM/Board接入说明.md`：
   - 说明 `*SET EVT=ON/OFF`。
   - 说明错误返回。
   - 说明蜂鸣器当前限制。

### 验证结果

已执行本机 C 语法检查：

```powershell
gcc -std=c99 -Wall -Wextra -fsyntax-only -DPART_TM4C1294NCPDT -I.\ARM\inc -I.\ARM\driverlib .\ARM\exp3-1.c
```

结果：通过。

### 上板复测建议

重新 Build/Download 后，先测：

```text
*GET TIME
*GET DATE
*GET DISP
*SET TIME=19:56:00
*SET DATE=2026-06-10
*SET EVT=ON
*SET EVT=OFF
*SET BEEP=ON
*SET BEEP=OFF
```

其中 `*SET EVT=OFF` 后应停止每秒刷屏；`*SET BEEP=ON` 如果没有蜂鸣声但 PF0/LED 有变化，说明仍需真实蜂鸣器管脚资料。

## 2026-06-10 蜂鸣器资料核对

### 本次目标

根据用户补充的 S800 图片、蓝板原理图和红板原理图，继续确认蜂鸣器真实接线。

### 已查看资料

- `D:/Download/S800图.JPG`
- `D:/Download/TM4C_SUBBOARD_0414.pdf`
- `D:/Download/TM4C_SUBBOARD_0414_dimension.pdf`
- `D:/Download/EK-TM4C1294XL REV D Schematic.pdf`

### 结论

1. `S800图.JPG` 中蜂鸣器旁边有 `BEEP` 丝印。
2. `TM4C_SUBBOARD_0414.pdf` 中，`BEEP` 模块由 `R70 -> Q9(S8050)` 驱动，输入网名为 `PWM7`。
3. 蓝板同一张原理图显示 `PWM0-3` 对应 `PF0-PF3`，而 `PWM7` 在 X11 BoosterPack 接口后段。
4. `EK-TM4C1294XL REV D Schematic.pdf` 显示红板 X11 后段包含 `PA1`，该脚也是 UART0 TX / Target TXD。
5. 因此蜂鸣器很可能与当前 USB 虚拟串口发送线存在复用/冲突风险，不能直接把 `Board_BuzzerWrite()` 改到 PA1/PWM7，否则可能破坏 SSCOM 通信。

### 当前处理

本次未修改代码逻辑，只更新 `ARM/Board接入说明.md` 和 `工作日志.md`：

- 将蜂鸣器结论从“管脚未知”更新为“蓝板 BEEP 输入为 PWM7”。
- 明确记录 PA1/UART0 TX 冲突风险。
- 保持当前 `PF0` 蜂鸣占位输出，等待更明确的课程蜂鸣器例程或确认是否允许重设计串口通道。

## 2026-06-10 蜂鸣器例程接入

### 本次目标

根据用户补充的课程蜂鸣器例程 `D:/Download/Beep.c`，确认真实蜂鸣器 PWM 引脚，并把当前工程从 PF0 占位输出改为真实蜂鸣器输出。

### 例程结论

`Beep.c` 明确给出了蜂鸣器接法：

- GPIO 引脚：`PK5`
- 复用功能：`GPIO_PK5_M0PWM7`
- PWM 模块：`PWM0_BASE`
- PWM 发生器：`PWM_GEN_3`
- PWM 输出：`PWM_OUT_7` / `PWM_OUT_7_BIT`
- 周期：`8000`
- 占空比：`BeepPeriod / 4`，约 25%

这个结论推翻了上一轮“PWM7 可能落在 PA1/UART0 TX 附近”的疑虑。按课程例程，蜂鸣器走 `PK5 / M0PWM7`，不会占用 SSCOM 正在使用的 UART0。

### 已完成文件

- `ARM/exp3-1.c`
- `ARM/Board接入说明.md`
- `工作日志.md`

### 已完成内容

1. 在 `ARM/exp3-1.c` 中加入 `pwm.h`。
2. 新增蜂鸣器周期常量 `BEEP_PERIOD = 8000`。
3. 新增 `S800_PWM_Init()`：
   - 启用 `SYSCTL_PERIPH_PWM0`。
   - 设置 `PWM_SYSCLK_DIV_1`。
   - 配置 `PWM_GEN_3` 为 down-count 模式。
   - 设置 `PWM_OUT_7` 周期和 25% 占空比。
   - 默认关闭 `PWM_OUT_7_BIT` 输出。
4. 在 `S800_GPIO_Init()` 中：
   - 启用 `SYSCTL_PERIPH_GPIOK`。
   - 将 `PK5` 配置为 `GPIO_PK5_M0PWM7`。
   - 将 `PK5` 设置为 PWM 输出。
5. 将 `Board_BuzzerWrite()` 从 `GPIOF PIN0` 占位输出改为真实 `PWM0 OUT7` 输出。
6. 更新 `ARM/Board接入说明.md`，记录 `PK5 / M0PWM7` 接法和 SSCOM 测试方式。

### 验证结果

已执行本机 C 语法检查：

```powershell
gcc -std=c99 -Wall -Wextra -fsyntax-only -DPART_TM4C1294NCPDT -I.\ARM\inc -I.\ARM\driverlib .\ARM\exp3-1.c
```

结果：通过。

### 上板复测建议

重新在 Keil 中 Build/Download 后，先用 SSCOM 测：

```text
*SET BEEP=ON
*SET BEEP=OFF
```

预期现象：`*SET BEEP=ON` 后蜂鸣器持续发声，`*SET BEEP=OFF` 后停止。随后再设置闹钟，到点后蜂鸣器应按 250 ms 节奏间歇响，并在约 10 秒后自动停止；按 `FUNC` 也应立即停止。

## 2026-06-10 新版 FAQ/开发要求对照修订

### 本次目标

阅读用户补充的两份新资料，并对照当前工程补齐第三周 PC 联调前必须稳定的 MCU 协议基础。

已阅读资料：

- `FAQ_常见问题解析_V1.3.pdf`
- `MCU与PC端开发要求_V1.0.pdf`

同时生成了便于检索的文本资料：

- `docs/FAQ_常见问题解析_V1.3.txt`
- `docs/MCU与PC端开发要求_V1.0.txt`

### 发现的主要差距

1. 我们原先的 `*EVT:DISP` 输出为状态文字，例如 `*EVT:DISP TIME LEFT`，不符合 PC 镜像要求。新版要求为 `*EVT:DISP <8字符> <dpHex>`。
2. 我们原先的 `*EVT:LED` 带 `0x` 前缀，新版要求为 `*EVT:LED <hex2>`。
3. 我们原先默认关闭事件上报以便 SSCOM 调试，但 PC 镜像验收要求 1 Hz 全量心跳持续到达。
4. `*PING` 原先固定返回 `*PONG 0`，新版要求返回真实 `uptime_s`。
5. `*SET:BEEP` 原先是 `ON/OFF` 调试命令，新版要求为 `10-5000 ms` 远程蜂鸣。
6. 第三周 PC 端需要 `*SET:LED`、`*SET:MODE`、`*SET:WEA`、`*NTP SYNC` 等协同入口。
7. `*SET:KEY` 执行后不能回发 `*EVT:KEY`，否则 PC 虚拟按键会形成环回。

### 已完成文件

- `ARM/exp3-1.c`
- `ARM/Board接入说明.md`
- `完整实施方案.md`
- `docs/新资料对照与第三周计划.md`
- `工作日志.md`

### 已完成内容

1. `*EVT:DISP` 改为 `*EVT:DISP <8字符> <dpHex>`。
2. `*EVT:LED` 改为 `*EVT:LED <hex2>`。
3. 默认开启事件上报；保留 `*SET EVT=OFF` 作为 SSCOM 调试开关。
4. `*PING` 改为返回真实开机秒数。
5. `*SET:BEEP <ms>` 支持 10-5000 ms 定时蜂鸣，并兼容 `ON/OFF` 调试写法。
6. 增加 `*SET:LED <hex2>` LED 接管，`00` 或 `*RST` 退出接管。
7. 增加 `*SET:MODE DAY/NIGHT`，NIGHT 模式仅显示时分 4 位并保留心跳 LED。
8. 增加 `*SET:WEA <T> <COND>` 天气下发，支持 `SUN/CLD/OVC/RAI/SNO/FOG`。
9. 增加 `*NTP SYNC`，用于 PC NTP 对时成功后点亮 LED7。
10. `*SET:KEY <NAME>` 执行时抑制 `*EVT:KEY` 回环。
11. LED 位定义调整为：
    - LED0：系统心跳
    - LED1：闹钟使能/响铃
    - LED2：编辑模式
    - LED3：UART 收发
    - LED4：天气晴
    - LED5：雨雪
    - LED6：高温
    - LED7：NTP 同步
12. UART 行长度改为 64 字节，超长返回 `ERROR LINE TOO LONG`。
13. `*SET:TIME`、`*SET:DATE`、`*SET:ALARM` 增加参数名组合解析，支持 `HOUR/MIN/SEC`、`YEAR/MONTH/DATE` 的单参或多参组合。

### 验证结果

已执行本机 C 语法检查：

```powershell
gcc -std=c99 -Wall -Wextra -fsyntax-only -DPART_TM4C1294NCPDT -I.\ARM\inc -I.\ARM\driverlib .\ARM\exp3-1.c
```

结果：通过。

### 第三周准备

第三周将进入 PC 上位机开发，开工前固定阅读：

1. `完整实施方案.md`
2. `工作日志.md`
3. `大作业题目-学生版_V1.2.md`
4. `docs/新资料对照与第三周计划.md`

PC 端每次执行 Python 命令前先运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_host\verify_env.ps1
```
